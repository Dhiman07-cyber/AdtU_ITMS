events {
  worker_connections  4096;
  multi_accept        on;
  use                 epoll;
}

http {
  # ── Security: hide server version ────────────────────────────────────────
  server_tokens off;

  # ── Gzip Compression ──────────────────────────────────────────────────────
  gzip              on;
  gzip_vary         on;
  gzip_proxied      any;
  gzip_comp_level   6;
  gzip_types        text/plain text/css text/xml application/json application/javascript application/xml+rss application/atom+xml image/svg+xml;

  # ── SSL / TLS ─────────────────────────────────────────────────────────────
  ssl_protocols             TLSv1.2 TLSv1.3;
  ssl_ciphers               ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256;
  ssl_prefer_server_ciphers off;
  ssl_session_cache         shared:SSL:10m;
  ssl_session_timeout       1d;
  ssl_stapling              on;
  ssl_stapling_verify       on;

  # ── Security headers applied to all responses ────────────────────────────
  add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
  add_header X-Content-Type-Options    "nosniff"                                      always;
  add_header X-Frame-Options           "DENY"                                         always;
  add_header Referrer-Policy           "strict-origin-when-cross-origin"              always;
  add_header Permissions-Policy        "geolocation=(self), camera=(), microphone=()" always;

  # ── Upstream: WebSocket servers (Sticky session via ip_hash) ─────────────
  # RATIONALE (Phase-03): In-process WebSocket state (SessionManager, Subscriptions)
  # requires clients to land on the same node until Redis PubSub is introduced in Phase-04.
  upstream ws_backend {
    ip_hash;
    server ws1:3001 max_fails=3 fail_timeout=10s;
    server ws2:3001 max_fails=3 fail_timeout=10s;
    # For Multi-EC2 Node expansion, register additional node IPs/hostnames below:
    # server ec2-node-1.internal:3001 max_fails=3 fail_timeout=10s;
    # server ec2-node-2.internal:3001 max_fails=3 fail_timeout=10s;
    keepalive 256;
  }

  # ── Upstream: Next.js app (Round-robin / Stateless Compute) ─────────────
  # RATIONALE (Phase-03): Next.js app layer is stateless (auth via Firebase JWTs, state in Supabase).
  # Round-robin load balancing distributes traffic evenly across application compute nodes.
  upstream nextjs_backend {
    least_conn;
    server nextjs:3000 max_fails=3 fail_timeout=10s;
    # For Multi-EC2 Application expansion, register additional nodes below:
    # server app-node-1.internal:3000 max_fails=3 fail_timeout=10s;
    # server app-node-2.internal:3000 max_fails=3 fail_timeout=10s;
    keepalive 64;
  }

  # ── Upstream: Health / Metrics (Internal Monitoring Routing) ────────────
  upstream health_backend {
    server ws1:9090 max_fails=3 fail_timeout=10s;
    server ws2:9090 max_fails=3 fail_timeout=10s;
    keepalive 16;
  }

  # ── HTTP → HTTPS redirect ─────────────────────────────────────────────────
  server {
    listen      80;
    server_name itms.example.com localhost _;
    location /   { return 301 https://$host$request_uri; }
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
  }

  # ── HTTPS server ──────────────────────────────────────────────────────────
  server {
    listen      443 ssl http2;
    server_name itms.example.com localhost _;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    # ── WebSocket upgrade ──────────────────────────────────────────────────
    location /ws {
      proxy_pass            http://ws_backend;
      proxy_http_version    1.1;
      proxy_set_header      Upgrade     $http_upgrade;
      proxy_set_header      Connection  "upgrade";
      proxy_set_header      Host        $host;
      proxy_set_header      X-Real-IP   $remote_addr;
      proxy_set_header      X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header      X-Forwarded-Proto $scheme;
      proxy_read_timeout    86400s;
      proxy_send_timeout    86400s;
      proxy_buffering off;
      proxy_cache off;
    }

    # ── Health endpoints ──────────────────────────────────────────────────
    # Internal-only: accessible from Docker network and loopback.
    # Prometheus scrapes from within the same itms Docker bridge network.
    # External requests are denied with 403.
    location /health {
      # RFC1918 private ranges + loopback
      allow 127.0.0.1;
      allow 10.0.0.0/8;
      allow 172.16.0.0/12;
      allow 192.168.0.0/16;
      deny  all;

      proxy_pass         http://health_backend;
      proxy_http_version 1.1;
      proxy_set_header   Host $host;
    }

    # ── Prometheus metrics ────────────────────────────────────────────────
    # Internal-only: Prometheus container scrapes from within Docker network.
    # Never expose this endpoint to the public internet.
    location /metrics {
      allow 127.0.0.1;
      allow 10.0.0.0/8;
      allow 172.16.0.0/12;
      allow 192.168.0.0/16;
      deny  all;

      proxy_pass         http://health_backend;
      proxy_http_version 1.1;
      proxy_set_header   Host $host;
    }

    # ── Next.js app ────────────────────────────────────────────────────────
    location / {
      proxy_pass            http://nextjs_backend;
      proxy_http_version    1.1;
      proxy_set_header      Host              $host;
      proxy_set_header      X-Real-IP         $remote_addr;
      proxy_set_header      X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header      X-Forwarded-Proto $scheme;
      proxy_read_timeout    30s;
      proxy_connect_timeout 10s;
    }
  }
}
